/**
 * Reexporta el pipe `SanitizeHtmlPipe` para facilitar su consumo desde otros módulos.
 *
 * - Permite importar directamente desde el directorio base con:
 *   `import { SanitizeHtmlPipe } from '.../shared/pipes';`
 */
export * from './sanitize-html.pipe';
